import { Menu } from '@dashboard/api/menu.interface';

export const menuData: Menu = {
    items: [
        { name: 'Fetch',  route: '/apps' },
        { name: 'Create', route: '/apps/add' },
    ]
};

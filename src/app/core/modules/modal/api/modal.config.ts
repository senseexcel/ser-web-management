import { IOverlayConfig } from './modal-config.interface';

export const DEFAULT_OVERLAY_CONFIGURATION: IOverlayConfig = {

    panelClass: 'ser-overlay--modal',

    hasBackdrop: true,

    backdropClass: 'ser-overlay--backdrop'
};

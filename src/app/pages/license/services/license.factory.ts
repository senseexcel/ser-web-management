import { UserModel } from '../user/user.model';

export class LicenseFactory {

    public static createUserLicense(data) {
        const userModel = new UserModel();
    }

    public static createTokenLicense(data) {}
}

import { TestBed } from '@angular/core/testing';

describe('SMC Modules SER: ScriptProvider', () => {

    beforeEach(() => {
        TestBed.configureTestingModule({
            providers: [
            ]
        });
    });

    /**
     *  user validation
     */
    describe('should validate ser script', () => {
    });
});
